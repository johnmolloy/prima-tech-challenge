apiVersion: v1
kind: Service
metadata:
  name: {{ include "prima-tech-challenge.fullname" . }}
  labels:
    {{- include "prima-tech-challenge.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 5000
      protocol: TCP
      name: http
  selector:
    {{- include "prima-tech-challenge.selectorLabels" . | nindent 4 }}
