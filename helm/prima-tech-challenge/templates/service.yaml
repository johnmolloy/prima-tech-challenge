apiVersion: v1
kind: Service
metadata:
  name: {{ include "prima-tech-challenge.fullname" . }}
  labels:
    app: {{ include "prima-tech-challenge.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    app: {{ include "prima-tech-challenge.name" . }}